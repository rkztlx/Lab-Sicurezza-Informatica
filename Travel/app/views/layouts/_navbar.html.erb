<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  &nbsp;  &nbsp;

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link navit" href="/places">Places </a>
      </li>
    </ul>
  </div>
  &nbsp;&nbsp;

<% if !user_signed_in? %>
  <a class="btn btn-secondary" href="/users/sign_in"><i class="fa fa-sign-in"></i> &nbsp; Log in</a>
<% end %>


<% if user_signed_in? %>
  <div class="d-flex">
    <div class="btn-group">
      <button type="button" class="btn btn-secondary" onclick="window.location.href='/users/<%= current_user.id%>'"><a><i class="fa fa-address-card"></i> &nbsp; </a>Hi <%= current_user.email %></button>
      <a class="dropdown-item" href="/users/sign_out" data-method="delete"><i class="fa fa-sign-out"></i> &nbsp; Logout</a>
      </div>
    </div>
  </div>
<%end%>
</nav>
